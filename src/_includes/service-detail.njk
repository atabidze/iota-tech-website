{% extends "base.njk" %}

{% block content %}
<section class="page-section service-detail-page">
    <div class="container">
        <div class="detail-page-layout">
            
            <aside class="detail-page-sticky-info" data-aos="fade-right">
                <h1 class="detail-page-title">{{ title }}</h1>
                <div class="detail-page-description page-content">
                    {{ general_description | markdownify | safe }}
                </div>
            </aside>

            <main class="detail-page-cards">
                {% if detailed_list and detailed_list.length > 0 %}
                    {% for item in detailed_list %}
                        <div class="feature-card-item" 
                             style="background: rgba(255, 0, 0, 0.5); border-radius: 15px; padding: 2rem; backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px);">
                            <div class="feature-card-header" style="border-bottom: 1px solid rgba(255, 255, 255, 0.2); padding-bottom: 1rem; margin-bottom: 1rem;">
                                <div class="feature-card-icon">
                                    {% lucide item.icon %}
                                </div>
                                <h3 class="feature-card-title">{{ item.point }}</h3>
                            </div>
                            {% if item.description %}
                            <div class="feature-card-body">
                                <p>{{ item.description }}</p>
                            </div>
                            {% endif %}
                        </div>
                    {% endfor %}
                {% endif %}
            </main>

        </div>
    </div>
</section>
{% endblock %}