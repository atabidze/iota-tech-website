---
layout: base.njk
---
{# ვპოულობთ შესაბამის კატალოგს, რომ მისი სათაური და იკონი გამოვიყენოთ #}
{% set catalog = collections.catalogs | findCatalogBySlug(slug) %}

<section class="service-detail-section">
    <div class="container">
        <div class="service-detail-header" data-aos="fade-up">
            <div class="category-title-wrapper">
                {% if catalog.data.icon %}
                    <div class="icon-bg lavender">
                         {% include "animated-icons/" + catalog.data.icon + ".svg" %}
                    </div>
                {% endif %}
                <h1>{{ catalog.data.title }}</h1>
            </div>
        </div>
        <div class="service-detail-content">
            <ul class="main-description-list" data-aos="fade-up" data-aos-delay="100">
                {% for item in detailed_points %}
                    <li class="glass-panel">{{ item.point }}</li>
                {% endfor %}
            </ul>
        </div>
    </div>
</section>